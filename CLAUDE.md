For any kubernetes manifests you create please store in the manifests folder.

the file name format should be:
`<manifest_name>.<k8s_resource_type>.yaml`

the k3d cluster name as told by kubectl is:
`k3d-lk3d-cluster`